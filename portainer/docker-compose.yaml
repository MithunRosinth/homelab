services:
  portainer:
    image: portainer/portainer-ee:latest
    container_name: portainer
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /mnt/c/home-server-configs/portainer_data:/data
    ports:
      - 8000:8000
      - 9443:9443
    restart: unless-stopped